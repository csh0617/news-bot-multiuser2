"""정치인 뉴스 크롤링·분류 파이프라인 패키지.

깔때기(funnel) 구조:
    수집 → 1.동명이인 필터 → 2.관련성(주체 vs 단순언급) → 3.감성분류 → 저장

원칙:
    - 1·2단계는 전부 로컬 규칙 기반(외부 API 0회)으로 대량을 먼저 걸러낸다.
    - 3단계 감성분류도 로컬 한국어 감성사전 기반(외부 API 0회).
    - url_hash 중복 제거로 같은 기사는 두 번 처리하지 않는다.
"""
